import React from "react";
import Button from "../../../baseComponents/button/Button.tsx";

interface GameButtonProps {

}

const GameButton = ({}: GameButtonProps) => {
  return (
    <Button>

    </Button>
  );
};

export default GameButton;
